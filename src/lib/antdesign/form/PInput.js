import {h, resolveComponent} from "vue";
export default {
    name: 'PInput',
    props: {

    },
    setup () {
    },
    render(){
        return h(resolveComponent('a-input'),{
            }
        )
    }
}
